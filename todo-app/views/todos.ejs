<table>
    <tr>
        <th>SNo</th>
        <th>To-Do Item</th>
        <th>Due Date</th>
    </tr>
    <% for( var i=0; i < allTodos.length; i++) { %>
        <tr>
            <td class="h-10"><%= allTodos[i].id %></td>
            <td class="h-10"><%= allTodos[i].title %></td>
            <td class="h-10"><%= allTodos[i].dueDate %></td>
        </tr> 
    <% } %>
</table>
<br>
